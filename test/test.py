from random import random

# from ChangeName.Tools.nearst_dot import nearst_dot

class a:
    xxx = 11
    def aa(self, c):
        print(c, self.xxx)

def c(x, z):
    x( z)

if __name__ == '__main__':
    aaa = a()
    c(aaa.aa, 'world')



    # x = [random()*10 for _ in range(20)]
    # y = [random()*10 for _ in range(20)]
    #
    # x = sorted(x)
    # y = sorted(y)
    #
    # for x1, y1 in zip(x, y):
    #     print(x1, y1)
    #
    #
    #
    # a = 5
    # b = 4
    #
    #
    # print('+++')
    # print(nearst_dot(x, y, a, b))
    # nearst_dot(x, y, a, b) = 0, 0
    #
    #
